"use client";
import React from "react";
import { useRouter } from "next/navigation";

export default function BookSpaceListPage() {
  const router = useRouter();

  return <div>BookSpaceListPage</div>;
}