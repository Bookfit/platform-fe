export interface UserInfo {
  userId: number;
  socialType: string;
  socialUniqueId: string;
  email: string;
  thumbnail: string;
  joinAt: string;
  createdAt: string;
  updatedAt: string;
  refreshToken: string;
}
